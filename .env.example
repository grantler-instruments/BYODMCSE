# MQTT broker WebSocket URL used by the browser client.
# Local example (requires a WebSocket-capable broker, e.g. Mosquitto on port 9001):
VITE_MQTT_BROKER_URL=ws://localhost:9001
# VITE_MQTT_BROKER_URL=wss://user:password@your-broker.example.com:443
